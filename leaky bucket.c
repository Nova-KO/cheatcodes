#include<stdip.h>
#include<studio.h>
#define NOF_PACKETS 10
int rand(int a)
{
 int rn = (random()%10)%a;
 return rn ==0?1:m;
 }
 int main()
 {
   int packet sz[NOF_PACKETS],i,clk,b_size,o_rate,p_sz_rm=0,p_sz,p_time,op;
   for(i=0;i<NOF_PACKET;++i)
     packet_sz[i]=rand(6)*10;
   for(i=0;i<NOF_PACKETS;++i)
   printf()  
